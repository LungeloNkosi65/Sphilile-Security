@model IEnumerable<SphileSecurity.Models.AssignShift>

@{
    ViewBag.Title = "Index";
}

<h2>Employee Shifts</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employees.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Shift.ShiftName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employees.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Shift.ShiftName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignId })
        </td>
    </tr>
}

</table>
